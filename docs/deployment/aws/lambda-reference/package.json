{
  "name": "atlas-folder-order-lambda",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild handler.ts --bundle --platform=node --target=node20 --outfile=dist/handler.js --format=cjs",
    "package": "npm run build && cd dist && zip -r ../latest.zip ."
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-cloudfront": "^3.700.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@types/aws-lambda": "^8.10.145",
    "typescript": "^5.9.3"
  }
}
